services:
  api:
    build:
      dockerfile: Dockerfile.api
    container_name: api_app_ets
    env_file: .env
    expose:
      - "8082"
    environment:
      - VIRTUAL_HOST=api.vidhub.io.vn
      - LETSENCRYPT_HOST=api.vidhub.io.vn
      - LETSENCRYPT_EMAIL=ml2673572@gmail.com
      - VIRTUAL_PORT=8082
    networks:
      - app-network
    restart: unless-stopped
networks:
  app-network:
    external: true
    name: app-network